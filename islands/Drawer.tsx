import Component from "$store/components/ui/Drawer.tsx";
import type { Props } from "$store/components/ui/Drawer.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
